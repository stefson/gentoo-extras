
# HG changeset patch
# User Henri Sivonen <hsivonen@hsivonen.fi>
# Date 1787017018 0
# Node ID 069f5c911cf2f727dc9efd3152a27c8cb96ecdf1
# Parent  935fefe782c805895800b4768f062d42192f7159
Bug 2056118 - Step back in numeric mode if identical prefix ends with digit but only one of left or right starts with digit.  a=RyanVM DONTBUILD

Original Revision: https://phabricator.services.mozilla.com/D318712

Differential Revision: https://phabricator.services.mozilla.com/D319005


diff --git a/js/src/tests/non262/Intl/Collator/numeric.js b/js/src/tests/non262/Intl/Collator/numeric.js
new file mode 100644
--- /dev/null
+++ b/js/src/tests/non262/Intl/Collator/numeric.js
@@ -0,0 +1,14 @@
+/* Any copyright is dedicated to the Public Domain.
+ * http://creativecommons.org/publicdomain/zero/1.0/ */
+
+let collator = new Intl.Collator("en", { numeric: true });
+
+assertDeepEq(['599', '600', '599B', '59A', '599A', '333A'].sort(collator.compare),
+             ['59A', '333A', '599', '599A', '599B', '600']);
+assertDeepEq(['899', '900', '8A'].sort(collator.compare),
+             ['8A', '899', '900']);
+assertDeepEq(['1662', '16A', '1660', '1600'].sort(collator.compare),
+             ['16A', '1600', '1660', '1662']);
+
+if (typeof reportCompare === "function")
+  reportCompare(0, 0, "ok");
diff --git a/third_party/rust/icu_collator/src/comparison.rs b/third_party/rust/icu_collator/src/comparison.rs
--- a/third_party/rust/icu_collator/src/comparison.rs
+++ b/third_party/rust/icu_collator/src/comparison.rs
@@ -1557,19 +1557,23 @@ impl<'data> CollatorBorrowed<'data> {
                                 break;
                             }
 
                             if numeric_primary.is_some()
                                 && head_last_ce32.tag_checked() == Some(Tag::Digit)
                                 && ((left_ce32.tag_checked() == Some(Tag::Digit)
                                     && left_ce32.digit() == 0)
                                     || (right_ce32.tag_checked() == Some(Tag::Digit)
-                                        && right_ce32.digit() == 0))
+                                        && right_ce32.digit() == 0)
+                                    || ((left_ce32.tag_checked() == Some(Tag::Digit))
+                                        ^ (right_ce32.tag_checked() == Some(Tag::Digit))))
                             {
-                                // Avoid giving the zero the leading zero treatment.
+                                // Avoid giving the zero the leading zero treatment
+                                // and also reject the case where only one of
+                                // right and left is a digit.
                                 break;
                             }
 
                             // We're at a good boundary but could not make a quick primary comparison decision.
 
                             // Note: It might look like a good idea to cache the CE32s, but
                             // doing so actually makes things slower.
                             break 'prefix;


