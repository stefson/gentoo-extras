From 7e9266135b412639916762051449111e1e05959b Mon Sep 17 00:00:00 2001
From: stefson <herrtimson@yahoo.de>
Date: Sat, 8 Feb 2020 17:07:52 +0100
Subject: [PATCH] fix compilation with clang-10

this mitigates https://github.com/Return-To-The-Roots/s25client/issues/1185
---
 src/FileEntry.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/external/libsiedler2/src/FileEntry.cpp b/external/libsiedler2/src/FileEntry.cpp
index 458f064..a86157a 100644
--- a/external/libsiedler2/src/FileEntry.cpp
+++ b/external/libsiedler2/src/FileEntry.cpp
@@ -41,7 +41,7 @@ bool libsiedler2::operator<(const FileEntry& left, const FileEntry& right)
             return false;
     }
     // Both negative or same
-    for(auto const& it : boost::combine(left.name, right.name))
+    for(const auto it : boost::combine(left.name, right.name))
     {
         auto const lhs = tolower(it.get<0>());
         auto const rhs = tolower(it.get<1>());
